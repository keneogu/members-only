
<p>
  <strong>Post:</strong>
  <% if user_signed_in? %>
  <%= @post.user.name %>
  <% end %>
  <%= @post.title %>
  <%= @post.post %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
